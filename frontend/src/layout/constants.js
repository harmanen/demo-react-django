// Top bar height is fixed to calculate the remaining
// available height in the view dynamically. See useStyles
// in MainLayout
export const TOP_BAR_HEIGHT = 162;

// Fix tabs / menu container height in TopBar so that the
// calculation mentioned above works properly independet of
// device size
export const MENU_CONTAINER_HEIGHT = 74;
